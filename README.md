# RETIRED — Casuar tiny causal core

This standalone prototype has been folded into the canonical monorepo:

`fedorivanenko/casuar`

Do not deploy or extend this repository. The maintained runtime lives under `causal_runtime/` in the monorepo; the canonical database is the fresh `casuar-core` Supabase project (`itsszsqxjqzaecqlbidk`).

Historical commits remain here only for provenance.
